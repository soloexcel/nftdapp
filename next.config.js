/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // swcMinify: false,
  images: {
    // domains: ['ipfs.infura.io'],
    domains: ['prowess.infura-ipfs.io'],
  },
};
 
module.exports = nextConfig;
